# {folder-name} — {the job in five words}

One job: {the single thing this folder does}.

## Inputs
- Working (this task): {the one file you were sent to}
- Reference (every task): `../_shared/{file}.md`

Do NOT load: {what an eager assistant would wrongly pull in}.

## Process
1. {Read the inputs.}
2. {Do the one job.}
3. {Save back to the same file.}

## Human check
{One concrete act a person does before the output is trusted.}
